{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": [
        "@playwright/mcp",
        "--executable-path", "/opt/google/chrome/chrome",
        "--headless",
        "--no-sandbox",
        "--isolated"
      ]
    }
  }
}
