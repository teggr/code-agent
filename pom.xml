<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.teggr.codeagent</groupId>
  <artifactId>code-agent</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <packaging>pom</packaging>

  <name>code-agent</name>
  <description>Service enabling developers to work with their repos, and drive AI coding agents, both locally and in the cloud.</description>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.release>25</maven.compiler.release>
    <spring-boot.version>4.1.1</spring-boot.version>
    <copilot-sdk-java.version>1.0.13-preview.1</copilot-sdk-java.version>
    <docker-java.version>3.4.0</docker-java.version>
    <docker-maven-plugin.version>0.46.0</docker-maven-plugin.version>
    <!-- Copilot CLI release baked into the runner image: "latest" or a tag such as v0.0.369 -->
    <copilot-cli.version>latest</copilot-cli.version>
    <mise.version>2025.1.3</mise.version>
    <!-- Browser automation baked into the runner image: system Chrome + Playwright MCP. -->
    <playwright.version>1.62.1</playwright.version>
    <playwright-mcp.version>0.0.80</playwright-mcp.version>
    <!-- Docker CLI baked into the runner image: "latest" or a version string such as 5:29.7.2-1~ubuntu.24.04~noble -->
    <docker-cli.version>latest</docker-cli.version>
    <!-- GitHub CLI baked into the runner image: "latest" or a version string such as 2.67.0 -->
    <gh-cli.version>latest</gh-cli.version>
    <docker.skip>false</docker.skip>
  </properties>

  <modules>
    <module>code-agent-app</module>
    <module>code-agent-runner</module>
  </modules>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring-boot.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>com.github</groupId>
        <artifactId>copilot-sdk-java</artifactId>
        <version>${copilot-sdk-java.version}</version>
      </dependency>
      <dependency>
        <groupId>com.github.docker-java</groupId>
        <artifactId>docker-java-core</artifactId>
        <version>${docker-java.version}</version>
      </dependency>
      <!-- Zerodep transport: the httpclient5 transport silently falls back to TCP with httpclient5 5.6.x. -->
      <dependency>
        <groupId>com.github.docker-java</groupId>
        <artifactId>docker-java-transport-zerodep</artifactId>
        <version>${docker-java.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.springframework.boot</groupId>
          <artifactId>spring-boot-maven-plugin</artifactId>
          <version>${spring-boot.version}</version>
        </plugin>
        <plugin>
          <groupId>io.fabric8</groupId>
          <artifactId>docker-maven-plugin</artifactId>
          <version>${docker-maven-plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <configuration>
            <parameters>true</parameters>
          </configuration>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
</project>
